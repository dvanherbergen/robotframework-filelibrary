*** Setting ***
Library    FileLibrary

*** Variables ***
${RESOURCE_DIR}       ${CURDIR}/resources
${OUT_DIR}            ${CURDIR}/../target


*** Test Cases ***
Verify file can be created from template
    Comment    "TODO..."
